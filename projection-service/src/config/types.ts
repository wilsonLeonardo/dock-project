export interface Envs {
  APPLICATION_NAME: string;
  APP_ENVIRONMENT: string;
  KAFKA_BROKER: string;
  KAFKA_TOPIC: string;
  DB_NAME: string;
  DB_USER: string;
  DB_PASSWORD: string;
  DB_HOST: string;
  DB_TYPE: string;
}
